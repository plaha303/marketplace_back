volumes:
  marketplace_db_data:
  redis_data:

services:
  marketplace_database:
    image: postgres:alpine
    container_name: marketplace_database
    restart: unless-stopped
    volumes:
      - marketplace_db_data:/var/lib/postgresql/data
      - ./infrastructure/postgres/init.sql:/docker-entrypoint-initdb.d/01-init.sql
    ports:
      - "5432:5432"
    expose:
      - "5432"
    env_file:
      - backend_api/.env
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      SCHEMA: public
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  redis:
    image: redis:alpine
    container_name: marketplace_redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    expose:
      - "6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  user_service:
    build:
      context: ./backend_api/user_service
      dockerfile: Dockerfile
    container_name: user_service
    restart: unless-stopped
    ports:
      - "8000:8000"
    expose:
      - "8000"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      DB_HOST: marketplace_database
      DB_PORT: 5432
      REDIS_URL: redis://marketplace_redis:6379/0
      SECRET_KEY: ${SECRET_KEY}
      DEBUG: ${DEBUG}
      ALLOWED_HOSTS: user_service,localhost,127.0.0.1
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    volumes:
      - ./backend_api/user_service:/app
      - ./backend_api/user_service/staticfiles:/app/staticfiles
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             python manage.py makemigrations app &&
             python manage.py migrate --noinput &&
             python manage.py collectstatic --noinput &&
             gunicorn --bind 0.0.0.0:8000 wsgi:application"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  order_service:
    build:
      context: ./backend_api/order_service
      dockerfile: Dockerfile
    container_name: order_service
    restart: unless-stopped
    ports:
      - "8001:8000"
    expose:
      - "8000"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      DB_HOST: marketplace_database
      DB_PORT: 5432
      REDIS_URL: redis://marketplace_redis:6379/0
      SECRET_KEY: ${SECRET_KEY}
      DEBUG: ${DEBUG}
      ALLOWED_HOSTS: order_service,localhost,127.0.0.1
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    volumes:
      - ./backend_api/order_service:/app
      - ./backend_api/order_service/staticfiles:/app/staticfiles
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             python manage.py makemigrations app &&
             python manage.py migrate --noinput &&
             python manage.py collectstatic --noinput &&
             gunicorn --bind 0.0.0.0:8000 wsgi:application"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  order_service_celery:
    build:
      context: ./backend_api/order_service
      dockerfile: Dockerfile
    container_name: order_service_celery
    restart: unless-stopped
    volumes:
      - ./backend_api/order_service:/app
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             celery -A celery_app worker -l info -Q default,orders"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      REDIS_URL: redis://marketplace_redis:6379/0
      DATABASE_URL: postgres://${DB_USERNAME}:${DB_PASSWORD}@marketplace_database:5432/${DB_DATABASE}?options=-c%20search_path=order_service
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    healthcheck:
      test: ["CMD", "celery", "-A", "celery_app", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  order_service_celery_beat:
    build:
      context: ./backend_api/order_service
      dockerfile: Dockerfile
    container_name: order_service_celery_beat
    restart: unless-stopped
    volumes:
      - ./backend_api/order_service:/app
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             celery -A celery_app beat -l info"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      REDIS_URL: redis://marketplace_redis:6379/0
      DATABASE_URL: postgres://${DB_USERNAME}:${DB_PASSWORD}@marketplace_database:5432/${DB_DATABASE}?options=-c%20search_path=order_service
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    healthcheck:
      test: ["CMD", "ps", "aux", "|", "grep", "celery"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  payment_service:
    build:
      context: ./backend_api/payment_service
      dockerfile: Dockerfile
    container_name: payment_service
    restart: unless-stopped
    ports:
      - "8002:8000"
    expose:
      - "8000"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      DB_HOST: marketplace_database
      DB_PORT: 5432
      REDIS_URL: redis://marketplace_redis:6379/0
      SECRET_KEY: ${SECRET_KEY}
      DEBUG: ${DEBUG}
      ALLOWED_HOSTS: payment_service,localhost,127.0.0.1
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    volumes:
      - ./backend_api/payment_service:/app
      - ./backend_api/payment_service/staticfiles:/app/staticfiles
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             python manage.py makemigrations app &&
             python manage.py migrate --noinput &&
             python manage.py collectstatic --noinput &&
             gunicorn --bind 0.0.0.0:8000 wsgi:application"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  payment_service_celery:
    build:
      context: ./backend_api/payment_service
      dockerfile: Dockerfile
    container_name: payment_service_celery
    restart: unless-stopped
    volumes:
      - ./backend_api/payment_service:/app
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             celery -A celery_app worker -l info -Q default,payments"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      REDIS_URL: redis://marketplace_redis:6379/0
      DATABASE_URL: postgres://${DB_USERNAME}:${DB_PASSWORD}@marketplace_database:5432/${DB_DATABASE}?options=-c%20search_path=payment_service
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    healthcheck:
      test: ["CMD", "celery", "-A", "celery_app", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  payment_service_celery_beat:
    build:
      context: ./backend_api/payment_service
      dockerfile: Dockerfile
    container_name: payment_service_celery_beat
    restart: unless-stopped
    volumes:
      - ./backend_api/payment_service:/app
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             celery -A celery_app beat -l info"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      REDIS_URL: redis://marketplace_redis:6379/0
      DATABASE_URL: postgres://${DB_USERNAME}:${DB_PASSWORD}@marketplace_database:5432/${DB_DATABASE}?options=-c%20search_path=payment_service
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    healthcheck:
      test: ["CMD", "ps", "aux", "|", "grep", "celery"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  product_service:
    build:
      context: ./backend_api/product_service
      dockerfile: Dockerfile
    container_name: product_service
    restart: unless-stopped
    ports:
      - "8004:8000"
    expose:
      - "8000"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      DB_HOST: marketplace_database
      DB_PORT: 5432
      REDIS_URL: redis://marketplace_redis:6379/0
      SECRET_KEY: ${SECRET_KEY}
      DEBUG: ${DEBUG}
      ALLOWED_HOSTS: product_service,localhost,127.0.0.1
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
      DATABASE_URL: postgres://${DB_USERNAME}:${DB_PASSWORD}@marketplace_database:5432/${DB_DATABASE}?options=-c%20search_path=product_service
    volumes:
      - ./backend_api/product_service:/app
      - ./backend_api/product_service/staticfiles:/app/staticfiles
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             python manage.py makemigrations app &&
             python manage.py migrate --noinput &&
             python manage.py collectstatic --noinput &&
             gunicorn --bind 0.0.0.0:8000 wsgi:application"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  product_service_celery:
    build:
      context: ./backend_api/product_service
      dockerfile: Dockerfile
    container_name: product_service_celery
    restart: unless-stopped
    volumes:
      - ./backend_api/product_service:/app
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             celery -A celery_app worker -l info -Q images,auto_moderation"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      REDIS_URL: redis://marketplace_redis:6379/0
      DATABASE_URL: postgres://${DB_USERNAME}:${DB_PASSWORD}@marketplace_database:5432/${DB_DATABASE}?options=-c%20search_path=product_service
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    healthcheck:
      test: ["CMD", "celery", "-A", "celery_app", "inspect", "ping"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  product_service_celery_beat:
    build:
      context: ./backend_api/product_service
      dockerfile: Dockerfile
    container_name: product_service_celery_beat
    restart: unless-stopped
    volumes:
      - ./backend_api/product_service:/app
    command: >
      sh -c "./wait-for-db.sh marketplace_database 5432 &&
             celery -A celery_app beat -l info"
    depends_on:
      marketplace_database:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - backend_api/.env
    environment:
      DJANGO_SETTINGS_MODULE: settings
      REDIS_URL: redis://marketplace_redis:6379/0
      DATABASE_URL: postgres://${DB_USERNAME}:${DB_PASSWORD}@marketplace_database:5432/${DB_DATABASE}?options=-c%20search_path=product_service
      ORDER_SERVICE_URL: http://order_service:8000
      USER_SERVICE_URL: http://user_service:8000
      PAYMENT_SERVICE_URL: http://payment_service:8000
      PRODUCT_SERVICE_URL: http://product_service:8000
    healthcheck:
      test: ["CMD", "ps", "aux", "|", "grep", "celery"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

networks:
  default:
    driver: bridge